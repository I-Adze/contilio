import React from "react";
import { render } from "@testing-library/react";
import App from "./App";

/**
 * I left this file in with the noble intent to update it once I'd written some code, but as this
 * was just an exercise I decided not to do that and so just left it as-is
 */
test("renders App", () => {
  render(<App />);
});
